# HTTP
httpx>=0.27.0
tenacity>=8.2.3

# Polymarket
py-clob-client>=0.17.0

# Database
# sqlite3 is in stdlib, no install needed

# Scheduling
APScheduler>=3.10.4

# Configuration
python-dotenv>=1.0.1

# Testing
pytest>=8.2.0
pytest-mock>=3.14.0

# Dashboard
streamlit>=1.35.0
plotly>=5.22.0
pandas>=2.2.0

# Probability / statistics (temperature distribution modeling)
scipy>=1.13.0
numpy>=1.26.0

# NOAA NBM quantile forecasts (US cities only)
# Requires system-level eccodes: brew install eccodes (macOS) or apt install libeccodes-dev (Linux)
# Install with: pip install herbie-data
# If not installed, NOAA NBM source is skipped gracefully — Open-Meteo ensemble is used instead.
herbie-data>=2024.3.0