# ============================================================
# Polymarket Weather Trading Bot — Python Dependencies
# Install: pip install -r requirements.txt
# ============================================================

# HTTP client + retry logic
httpx>=0.27.0
tenacity>=8.2.3

# Polymarket CLOB (order placement for live trading).
# py_clob_client_v2 is REQUIRED — the original py-clob-client (v1) has
# deprecated exchange contract addresses hardcoded in its config; orders
# signed against those contracts get rejected with `order_version_mismatch`.
# v2 has the current contracts and the new order schema.  The v1 package
# is intentionally not pinned here — nothing imports it anymore.
py_clob_client_v2>=1.0.0

# Scheduling
APScheduler>=3.10.4

# Configuration
python-dotenv>=1.0.1

# Dashboard
streamlit>=1.35.0
streamlit-autorefresh>=1.0.1
plotly>=5.22.0
pandas>=2.2.0

# Probability / statistics (temperature distribution modeling)
scipy>=1.13.0
numpy>=1.26.0

# Machine learning — per-city temperature distribution model (Framing C)
# HistGradientBoostingRegressor + CalibratedClassifierCV live in sklearn.
# joblib is a transitive dep used for model persistence.
scikit-learn>=1.4.0
joblib>=1.3.0

# Sunrise/sunset calculation (sigma shrinkage time-of-day scaling)
astral>=3.2

# Timezone resolution from lat/lon coordinates
timezonefinder>=6.2.0

# WebSocket (real-time price streaming from Polymarket CLOB)
websockets>=12.0

# Testing
pytest>=8.2.0
pytest-mock>=3.14.0
